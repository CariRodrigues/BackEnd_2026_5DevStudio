extends layout

block content

  h2 Lista de productos

  table(border="1")
    tr
      th ID
      th Nombre
      th Precio
      th Stock
      th Acciones

    each producto in productos
      tr
        td= producto.id
        td= producto.nombre
        td= producto.precio
        td= producto.stock
        td
          a(href=`/productos/vista/${producto.id}`) Ver
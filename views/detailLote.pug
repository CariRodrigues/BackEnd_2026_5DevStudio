extends ./layout

block content
  .card-container
    .card-header
      h1 Detalle del Lote

    .form-group
      strong Código:
      |  #{lote.codigoLote}
    .form-group
      strong Producto:
      |  #{lote.producto ? lote.producto.nombre : "-"}
    .form-group
      strong Proveedor:
      |  #{lote.proveedor ? lote.proveedor.nombre : "-"}
    .form-group
      strong Fecha de ingreso:
      |  #{new Date(lote.fechaIngreso).toLocaleDateString()}
    .form-group
      strong Fecha de vencimiento:
      |  #{new Date(lote.fechaVencimiento).toLocaleDateString()}
    .form-group
      strong Cantidad disponible:
      |  #{lote.cantidadDisponible}
    .form-group
      strong Cantidad inicial:
      |  #{lote.cantidadInicial}
    .form-group
      strong Estado:
      |  #{lote.estado}

    .form-actions
      a.button(href="/lotes/vista") Volver

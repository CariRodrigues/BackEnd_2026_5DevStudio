extends layout

block content

    h2 403 - Acceso denegado
    p No tenés permisos para acceder a esta sección.

    if usuario
        p Usuario: #{usuario.nombre}
        p Rol: #{usuario.rol}

    a.button(href="/productos/vista") Volver al sistema